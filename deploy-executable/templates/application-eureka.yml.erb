---
spring:
  profiles: default
  application:
    name: <%= dist_name %>
  
server:
  port: 8080

eureka:
  instance:
    hostname: <%%= node['cloud']['local_hostname'] %>
  client:
    serviceUrl:
      defaultZone: <%%= @eureka_hosts.join(',') %>
