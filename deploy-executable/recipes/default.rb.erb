node.override['app_deploy'] = node['<%= package_attribute_collection_name %>']

# install the package
include_recipe "supervised_app::install"

# your install path is now: node['install_path']
# you can expand templates, etc relative to this path
## my stuff here


# start the package
include_recipe "supervised_app::restart"